<!DOCTYPE html>
<html>
  <head>
    <title>FavMovieLineCapstone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <% flash.each do |name,msg| %>
      <%= content_tag :div, msg, id: "flash_#{name}" %>
      <% end %>

      <% if current_user %>

      <div class="container-fluid">
        <div class="row">
            <aside class="col-2">
              <div class="row d-flex  align-items-center p-3">
                   <%= image_tag current_user.cover_pic ,:class=> 'img-fluid'%>
                   <span class="font-weight-bold ml-2 text-muted"><%= current_user.username %></span>
              </div>
               <div class="row px-2">
                  <div class="col-6 d-flex flex-column align-items-center justify-content-center">
                    <p class="m-0">10</p>
                    <p class="m-0">Following</p>
                  </div>
                  <div class="col-6 d-flex flex-column align-items-center justify-content-center">
                    <p class="m-0">12</p>
                    <p class="m-0">Followers</p>
                  </div>
               </div>
               <div class="row d-flex align-items-center px-3 py-2">
                  <i class="fa fa-home p-2" aria-hidden="true"></i>
                  <span><%= link_to 'Home', lines_path %> </span>
               </div>
               <div class="row d-flex align-items-center px-3 py-2">
                  <i class="fas fa-sign-out-alt p-2"></i>
                  <span><%= link_to 'Log Out', logout_path%></span>
               </div>
            </aside>

            <div class="page  p-0 col-10" >
                <div class="navb w-100">
                  <%= user_actions(current_user) %>
                  <%=link_to('Home', lines_path)%>
                </div>
                <div class="row p-0 m-0">
                      <%= yield %>
                </div>
              </div>

        </div>
                 
      <div>
      <% else %>
        <%= yield %>
        <% end %>
  </body>
</html>
